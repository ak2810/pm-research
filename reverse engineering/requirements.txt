# ── Core data / IO ────────────────────────────────────────────────────────────
polars==0.20.31
pyarrow==16.1.0
boto3==1.34.144
numpy==2.4.4
pandas==3.0.2
pydantic==2.7.4
pydantic-settings==2.3.4
structlog==24.2.0
python-dotenv==1.2.2

# ── Statistical modeling ──────────────────────────────────────────────────────
scipy==1.17.1
scikit-learn==1.8.0
statsmodels==0.14.6
arch==8.0.0

# ── Gradient-boosted trees (Layer 3) ─────────────────────────────────────────
# LightGBM: CPU-only on Windows via pip (GPU requires source build; see DECISIONS.md)
lightgbm==4.6.0
xgboost==3.2.0
shap==0.51.0

# ── Deep learning (Layer 4 — LSTM/Transformer) ────────────────────────────────
# CUDA 12.4 wheel; requires CUDA 13.1 driver (driver >= 12.4 is backward-compatible)
# Install: pip install torch==2.6.0+cu124 torchvision torchaudio
#          --index-url https://download.pytorch.org/whl/cu124
# (Cannot specify +cuXXX suffix in plain requirements.txt and have pip resolve it
#  automatically; run the install command above on first setup.)
torch==2.6.0+cu124

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2

# ── Notebooks ─────────────────────────────────────────────────────────────────
notebook==7.5.6
ipykernel==7.2.0

# ── Dev / QA ──────────────────────────────────────────────────────────────────
mypy==1.10.1
ruff==0.4.10
pytest==8.2.2
pytest-asyncio==0.23.7
