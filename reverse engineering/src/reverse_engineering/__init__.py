"""Reverse-engineering of @ohanism's trading strategy on Polymarket.

All computation runs locally (i7-11700K / 64 GB / RTX 3060).
EC2 is a data source only — never a compute node.
"""

__version__ = "0.1.0"
